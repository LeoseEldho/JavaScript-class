// Q1. Rotating Conveyor Belt Simulation
// A circular conveyor belt has N positions with values.
// Every minute:
// All values shift right by 1
// Value at last index moves to index 0
// After T minutes, return the final configuration.
// Input

// N = 5
// Belt = [3, 8, 1, 9, 4]
// T = 7
// Output
// [9, 4, 3, 8, 1]


// Q2. First Non-Repeating Window Character
// Given a string S and integer K,
//  for every substring of size K, print the first non-repeating character.
// If none exists, print #.
// Input

// S = "aabcdbec"
// K = 4
// Output
// a b c d d e


// Q3. Server Load Balancer
// You have N servers, each with current load.
// Tasks arrive one by one.
//  Each task must go to the server with minimum load.
// If tie → pick server with smallest index.
// After all tasks are assigned, return the final load array.
// Input

// Servers = [3, 3, 5]
// Tasks = [4, 2, 1, 3]
// Output
// [6, 6, 6]


// Q4. Minimum Steps to Equal Heights
// Given an array of tower heights.
// In one move, you may:
// Increase any one tower by 1
// OR decrease any one tower by 1
// Find the minimum number of moves to make all towers equal.
// Input
// [1, 5, 7]
// Output
// 6


// Q5. Secret Message Decoder
// A message is encoded as:
// Letters followed by digits mean repeat letter
// If no digit → appear once
// Input

// a2b3cd2
// Output
// aabbbcdcd


// Q6. Server Load Balancer
// You have N servers, each with current load.
// Tasks arrive one by one.
//  Each task must go to the server with minimum load.
// If tie → pick server with smallest index.
// After all tasks are assigned, return the final load array.
// Input

// Servers = [3, 3, 5]
// Tasks = [4, 2, 1, 3]
// output: [ 6, 7, 8 ]
